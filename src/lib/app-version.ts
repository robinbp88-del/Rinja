/**
 * User-facing app version. Bump when you ship a meaningful deploy
 * (patch for fixes, minor for beta features). Keep in sync with package.json.
 */
export const APP_VERSION = "0.1.1";

export const APP_VERSION_LABEL = `v${APP_VERSION}`;
